# Keep shell scripts LF so they run on the Linux CI runner
gradlew text eol=lf
*.sh text eol=lf

# Windows batch stays CRLF
*.bat text eol=crlf

# Binary assets — never munge line endings
*.png binary
*.jpg binary
*.jar binary
*.keystore binary
*.woff2 binary
